---
title: "Análise da Taxa de Mortalidade Infantil e Indicadores Socioeconômicos"
author:
  - Adriano Porto
  - Kauan Brito
date: today
lang: pt

format:
  html:
    theme: cosmo
    toc: true
    toc-depth: 3
    number-sections: true
  pdf: default

editor: visual
---

```{r}
#| label: setup
#| echo: false
#| message: false
#| warning: false

library(dplyr)
library(ggplot2)
library(readxl)
library(corrplot)
library(rlang)
library(skimr)
library(car)
library(easystats)
```

# Introdução

A taxa de mortalidade infantil (TMI) é um dos principais indicadores utilizados para avaliar as condições de saúde e desenvolvimento socioeconômico de uma população. Esse indicador reflete não apenas a qualidade da assistência médica disponível, mas também aspectos estruturais da sociedade, como condições de saneamento, renda, educação e desigualdade social.

Diversos fatores socioeconômicos podem influenciar diretamente os níveis de mortalidade infantil. Entre esses fatores destacam-se o acesso ao saneamento básico, o nível de renda da população, a cobertura de serviços de atenção primária à saúde, o nível educacional da população e indicadores de desigualdade econômica.

Neste estudo busca-se analisar a relação entre a taxa de mortalidade infantil e diferentes indicadores socioeconômicos e de infraestrutura de saúde. Para isso, será utilizada uma base de dados contendo variáveis relacionadas a condições econômicas, sociais e de acesso à saúde.

O objetivo principal desta análise é investigar possíveis associações entre a taxa de mortalidade infantil e variáveis socioeconômicas, além de realizar uma análise exploratória inicial da base de dados.

# Descrição da Base de Dados

A base de dados utilizada neste estudo contém informações relacionadas a indicadores socioeconômicos e de saúde pública. Cada observação corresponde a uma unidade geográfica, podendo representar estados ou municípios, contendo variáveis explicativas que podem influenciar a taxa de mortalidade infantil.

## Leitura da base de dados

```{r}
dados <- read_excel("dados.xlsx")

dados$regiao <- factor(
  dados$regiao,
  levels = c("Norte", "Nordeste", "Centro-Oeste", "Sudeste", "Sul")
)

head(dados)
```

## Número de observações

```{r}
nrow(dados)
```

## Estrutura da base de dados

```{r}
glimpse(dados)
```

## Estatísticas descritivas detalhadas

```{r}

skim(dados)
```

## Variáveis da base de dados

| Variável | Significado | Unidade |
|-----------------------|------------------------------|-------------------|
| tmi | Taxa de mortalidade infantil | óbitos por 1000 nascidos vivos |
| saneamento | Percentual da população com acesso a saneamento básico | \% |
| pib_percapita | Produto Interno Bruto per capita | R\$ |
| psf | Cobertura da Estratégia Saúde da Família | \% |
| analfabetismo | Taxa de analfabetismo | \% |
| gini | Índice de desigualdade de renda | 0 a 1 |
| idh | Índice de Desenvolvimento Humano | 0 a 1 |
| renda_percapita | Renda média per capita | R\$ |
| leitos | Número de leitos hospitalares | leitos por 1000 habitantes |
| regiao | Região geográfica do Brasil (Norte, Nordeste, Centro-Oeste, Sudeste, Sul) | categórica |

# Análise Exploratória de Dados

Antes da construção de modelos estatísticos, é importante realizar uma análise exploratória da base de dados. Essa etapa permite compreender melhor as características das variáveis, identificar possíveis valores ausentes e detectar a presença de valores extremos (outliers).

## Verificação de valores ausentes

```{r}
colSums(is.na(dados))
```

## Distribuição da Taxa de Mortalidade Infantil

```{r}
ggplot(dados, aes(x = tmi)) +
  geom_histogram(bins = 10, fill = "lightblue", color = "black") +
  labs(
    title = "Distribuição da Taxa de Mortalidade Infantil",
    x = "TMI",
    y = "Frequência"
  )
```

## Identificação de possíveis outliers

```{r}
boxplot(
  dados$tmi,
  main = "Boxplot da Taxa de Mortalidade Infantil",
  ylab = "TMI"
)
```

A análise exploratória permite identificar padrões iniciais nos dados e possíveis inconsistências na base de dados, como valores ausentes ou observações extremas.

# Análises Bivariadas

## Somente entre variáveis numéricas

```{r}
dados_num <- dados %>% select_if(is.numeric)

corr_matrix <- cor(dados_num, use = "complete.obs")

corr_matrix
```

```{r}
corrplot(
  corr_matrix,
  method = "color",
  type = "upper",
  addCoef.col = "black",
  number.cex = 0.65,
  tl.cex = 0.75,
  tl.srt = 45
)
```

A análise da matriz de correlação indica relações importantes entre as variáveis socioeconômicas e a taxa de mortalidade infantil (TMI).

Observa-se uma forte correlação negativa entre TMI e renda per capita (-0.89), indicando que regiões ou municípios com maior renda tendem a apresentar menores níveis de mortalidade infantil. De forma semelhante, o Índice de Desenvolvimento Humano (IDH) também apresenta correlação negativa elevada com a TMI (-0.83), sugerindo que melhores condições de desenvolvimento humano estão associadas a menores taxas de mortalidade infantil.

O acesso ao saneamento também apresenta correlação negativa significativa com a mortalidade infantil (-0.85), reforçando a importância da infraestrutura sanitária para a saúde pública.

Por outro lado, o analfabetismo apresenta correlação positiva com a TMI (0.76), indicando que locais com maiores níveis de analfabetismo tendem a registrar maiores taxas de mortalidade infantil.

Esses resultados sugerem que fatores socioeconômicos, educacionais e de infraestrutura estão fortemente associados às condições de saúde infantil.

## Variável numérica em função de categórica

```{r}
ggplot(dados, aes(x = regiao, y = tmi)) +
  geom_boxplot(fill = "lightblue") +
  theme_minimal() +
  labs(
    title = "Taxa de Mortalidade Infantil por Região do Brasil",
    x = "Região",
    y = "TMI"
  )
```

Observa-se que a taxa de mortalidade infantil apresenta diferenças importantes entre as cinco regiões do Brasil (Norte, Nordeste, Centro-Oeste, Sudeste e Sul).

As regiões Norte e Nordeste apresentam medianas mais elevadas de TMI, além de maior dispersão dos valores, indicando maior variabilidade entre os municípios dessas regiões.

Em contraste, as regiões Sul e Sudeste apresentam medianas significativamente menores, sugerindo melhores condições socioeconômicas e de acesso a serviços de saúde.

A região Centro-Oeste apresenta valores intermediários, situando-se entre os grupos de maior e menor mortalidade infantil.

Esses resultados indicam desigualdades regionais importantes nas condições de saúde e desenvolvimento.

## Relação entre renda e mortalidade infantil

```{r}
ggplot(dados, aes(x = renda_percapita, y = tmi)) +
  geom_point() +
  geom_smooth(method = "lm") +
  theme_minimal() +
  labs(
    title = "Renda per capita vs Mortalidade Infantil",
    x = "Renda per capita",
    y = "TMI"
  )
```

A relação entre renda per capita e mortalidade infantil apresenta um padrão claramente negativo.

Observa-se que à medida que a renda per capita aumenta, a taxa de mortalidade infantil tende a diminuir. A linha de regressão ajustada confirma essa tendência, indicando uma associação linear negativa entre as variáveis.

Esse resultado sugere que municípios com maior renda possuem melhores condições de vida, maior acesso a serviços de saúde, nutrição adequada e infraestrutura básica, fatores que contribuem para a redução da mortalidade infantil.

## Relação entre saneamento e mortalidade infantil

```{r}
ggplot(dados, aes(x = saneamento, y = tmi)) +
  geom_point() +
  geom_smooth(method = "lm") +
  theme_minimal() +
  labs(
    title = "Saneamento vs Mortalidade Infantil",
    x = "Acesso ao saneamento (%)",
    y = "TMI"
  )
```

A análise do gráfico indica uma relação negativa entre acesso ao saneamento e mortalidade infantil.

Observa-se que municípios com maior porcentagem de acesso ao saneamento tendem a apresentar menores taxas de mortalidade infantil. A linha de regressão reforça essa tendência, indicando uma associação inversa entre as variáveis.

Esse resultado evidencia a importância do saneamento básico para a saúde pública, uma vez que melhores condições sanitárias reduzem a exposição a doenças infecciosas e melhoram as condições de vida da população.

# Ajuste do Modelo Inicial de Regressão

Inicialmente foi ajustado um modelo de regressão linear múltipla considerando todas as variáveis explicativas disponíveis na base de dados.

```{r}
modelo_inicial <- lm(tmi ~ saneamento + pib_percapita + cobertura_psf + analfabetismo 
                     + gini + idh + renda_percapita + leitos_mil + regiao, data = dados)

summary(modelo_inicial)
```

Observa-se que o coeficiente de determinação ajustado (R² ajustado) obtido foi de 0.9493, indicando que aproximadamente 94,93% da variabilidade da TMI é explicada pelas variáveis independentes incluídas no modelo, o que sugere um bom nível de ajuste aos dados.

Temos também que a variável leitos_mil, que indica o número de leitos hospitalares por mil habitantes, apresenta valor-p elevado, indicando menor evidência de associação com a variável resposta do modelo.

## Diagnóstico de Multicolinearidade

```{r}
vif(modelo_inicial)
```

A análise de multicolinearidade do modelo inicial, através do VIF, indicou uma alta correlação entre pib_percapita e renda_percapita.

Esse resultado é esperado, visto que, além do que foi exposto pela matriz de correlação da análise bivariada dos dados, ambas representam indicadores de nível econômico da população,. Dessa forma, na etapa de seleção do melhor modelo, será considerada a remoção de uma dessas variáveis a fim de reduzir o problema de multicolinearidade.

# Seleção de Modelo

## Com ou Sem Leitos por Mil Habitantes

```{r}
modelo_sem_leitos <- update(modelo_inicial, . ~ . - leitos_mil)

summary(modelo_sem_leitos)
```

```{r}
AIC(modelo_inicial)
```
```{r}
AIC(modelo_sem_leitos)
```

Foi avaliada a remoção da variável leitos_mil, uma vez que essa variável apresentou p-valor elevado no modelo inicial. Para isso, foi ajustado um modelo alternativo excluindo essa variável.

Observou-se que o R² ajustado apresentou pequeno aumento, passando de aproximadamente 0.9493 para 0.9503, indicando leve melhora na proporção de variabilidade explicada pelo modelo.

Entretanto, ao comparar os modelos utilizando o Critério de Informação de Akaike (AIC), verificou-se que o modelo inicial apresentou AIC = 1392,245, enquanto o modelo sem a variável leitos_mil apresentou AIC = 1402,693.

Como a diferença nos valores de AIC foi maior que 10, conclui-se que a remoção da variável leitos por mil habitantes resultou em pior desempenho do modelo. Dessa forma, optou-se por manter essa variável no modelo.

## PIB per capita ou Renda per capita

### Apenas PIB per capita

```{r}
modelo_apenas_pib <- update(modelo_inicial, . ~ . - renda_percapita)

summary(modelo_apenas_pib)
```

### Apenas Renda per capita
```{r}
modelo_apenas_renda <- update(modelo_inicial, . ~ . - pib_percapita)

summary(modelo_apenas_renda)
```

### Comparação de valores AIC

```{r}
AIC(modelo_apenas_pib)
```

```{r}
AIC(modelo_apenas_renda)
```

Considerando a forte multicolinearidade observada entre as variáveis PIB per capita e Renda per capita, foram ajustados dois modelos alternativos: um mantendo apenas a variável PIB per capita e outro mantendo apenas a variável renda per capita.

Observou-se que o modelo contendo apenas PIB per capita apresentou R² ajustado = 0,9487 e AIC = 1394,935, enquanto o modelo contendo apenas renda per capita apresentou R² ajustado = 0,9493 e AIC = 1391,534.

Como o modelo com renda per capita apresentou maior poder explicativo e menor valor de AIC, conclui-se que ele apresenta melhor desempenho. Dessa forma, optou-se por manter a variável renda per capita e remover a variável PIB per capita, resultando em um modelo mais parcimonioso e estatisticamente adequado.

```{r}
modelo_final <- update(modelo_inicial, . ~ . - pib_percapita)

vif(modelo_final)
```

Observou-se que as variáveis de renda e analfabetismo apresentaram valores VIF maiores que 5, porém inferiores a 10, indicando multicolinearidade moderada.

Tal resultado é esperado, uma vez que ambas as variáveis representam indicadores socioeconômicos relacionados ao nível de desenvolvimento, sendo plausível a existência de correlação entre elas. Como os valores de VIF permanecem abaixo do limite crítico frequentemente utilizado (VIF > 10), conclui-se que não há evidência de multicolinearidade severa capaz de comprometer a estabilidade das estimativas do modelo.

Dessa forma, optou-se por manter ambas as variáveis no modelo, considerando sua relevância interpretativa para explicar a variabilidade da taxa de mortalidade infantil.

# Checando pressupostos do MRLM

## Plot do R base
```{r}
plot(modelo_final)
```

## Checagem manual do Easystats
```{r}
check_normality(modelo_final)
```

```{r}
check_heteroscedasticity(modelo_final)
```

```{r}
check_outliers(modelo_final)
```

```{r}
check_collinearity(modelo_final)
```

```{r}
check_autocorrelation(modelo_final)
```

## Análise gráfica de todos os pressupostos
```{r, fig.width=5, fig.height=5}
check_model(modelo_final)
```